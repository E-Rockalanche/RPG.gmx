#define Position
/// Position(x, y)
/*
encodes a coordinate into a single value
*/

return argument0 + (argument1 << 16);

#define PositionX
/// PositionX(position)

return argument0 & $ffff;

#define PositionY
/// PositionY(position)

return argument0 >> 16;